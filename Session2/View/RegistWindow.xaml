<Window x:Class="Desktop.View.RegistWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:materialIcons ="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm ="clr-namespace:Desktop.ViewModel"
        xmlns:local="clr-namespace:Desktop.View"
        xmlns:util="clr-namespace:Desktop.Utilits"
        mc:Ignorable="d"
        Title="RegistWindow" Height="600" Width="450"
        Left="450"
        Top="100"
        WindowState="{Binding WindowState}">
    <Window.DataContext>
        <vm:RegistViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid Grid.ColumnSpan="3" Grid.Row="0" HorizontalAlignment="Right">
            <StackPanel Orientation="Horizontal">
                <Button Width="50" Height="60" Command="{Binding StateminCommand}" 
            Background="Transparent" BorderBrush="Transparent" 
            Foreground="BlueViolet" Content="{materialIcons:MaterialIconExt Kind=Minus, Size=20}" 
            VerticalContentAlignment="Top" VerticalAlignment="Top" HorizontalContentAlignment="Center" 
            HorizontalAlignment="Right" Margin="0 0 0 10"/>
                <Button Width="50" Height="60" Command="{Binding StatemaxCommand}" 
            Background="Transparent" BorderBrush="Transparent" 
            Foreground="BlueViolet" Content="{materialIcons:MaterialIconExt Kind=Maximize, Size=20}" 
            VerticalContentAlignment="Top" VerticalAlignment="Top" HorizontalContentAlignment="Center" 
            HorizontalAlignment="Right" Margin="0 0 0 10"/>
                <Button  Width="50" Height="60" Background="Transparent" BorderBrush="Transparent" Foreground="BlueViolet" 
                         Content="{materialIcons:MaterialIconExt Kind=Close, Size=20}" 
                         VerticalContentAlignment="Top" HorizontalContentAlignment="Center" 
            HorizontalAlignment="Right" VerticalAlignment="Top" Click="Button_Click_1" ></Button>
            </StackPanel>

        </Grid>

        <TextBlock Grid.Column="1" Grid.Row="1" Margin="10" HorizontalAlignment="Center" 
                   VerticalAlignment="Center" FontSize="30">Создать аккаунт</TextBlock>
        <TextBox Grid.Column="1" Grid.Row="2" Margin="10" x:Name="Email" 
                 materialDesign:HintAssist.Hint="Электронная почта"
                 Text="{Binding SelectedEmployee.Email, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged}"/>
        <PasswordBox Grid.Column="1" Grid.Row="3" Margin="10" x:Name="Password" 
                     materialDesign:HintAssist.Hint="Пароль"
                     util:PasswordBoxHelper.Attach="True"
                     util:PasswordBoxHelper.Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     />
        <PasswordBox Grid.Column="1" Grid.Row="4" Margin="10" x:Name="RepeatPassword" 
                     materialDesign:HintAssist.Hint="Повтор пароля"
                     util:PasswordBoxHelper.Attach="True"
                     util:PasswordBoxHelper.Password="{Binding RepeatPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     />
        <StackPanel Orientation="Horizontal"  Grid.Column="1" Grid.Row="6" Margin="5" 
                    HorizontalAlignment="Center">
            <TextBlock>Уже есть аккаунт?    </TextBlock>
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Top">
   
                <Hyperlink Click="Hyperlink_MouseDown_1" >Войти</Hyperlink>
            </TextBlock>
        </StackPanel>

        <Button  Grid.Column="1" Grid.Row="5" Margin="10 5" Command="{Binding CreateCommand}">
            Зарегистрироваться
        </Button>
    </Grid>
</Window>
